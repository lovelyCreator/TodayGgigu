# Copy to keystore.properties (gitignored) and adjust paths if your keystore lives elsewhere.
# Place the release keystore at android/app/todaymall.keystore OR set storeFile below.

storeFile=app/todaymall.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=todaymall_alias
keyPassword=YOUR_KEY_PASSWORD
