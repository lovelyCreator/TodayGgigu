<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera permission — required to take photos directly within the app
         (image search, refund request photos, profile photo capture). -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!--
      Google Play 정책 (Photo and Video Permissions Policy) 준수:
      사진/동영상은 일회성으로만 사용 (검색용 업로드, 환불 사진 첨부 등) 하므로
      `READ_MEDIA_IMAGES` / `READ_MEDIA_VIDEO` 권한을 보유하지 않고 Android
      시스템의 Photo Picker (ACTION_PICK_IMAGES) 만 사용한다.

      react-native-image-picker 의 launchImageLibrary 는 Android 13+ 에서
      Photo Picker 를 자동 사용하므로 권한 선언 없이 동작한다.

      ─ 의존 라이브러리(react-native-image-picker, react-native-image-crop-picker
         등) 의 자체 AndroidManifest 가 다음 권한들을 자동 병합하지 않도록
         `tools:node="remove"` 로 명시 제거.
      ─ Android 12 이하용 레거시 저장소 권한도 제거 — 이미지 선택은
         system picker 가 일회성 grant 로 처리.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" tools:node="remove" />

    <!-- Camera feature declaration -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
